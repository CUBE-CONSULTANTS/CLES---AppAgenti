# Getting Started

1. Install dependencies:
   ```bash
   npm install
   ```

2. deploy the database:
   ```bash
   cds deploy
   ```

3. Start the application:
   ```bash
   cds start
   ```

# Access the application

You can access the application at `http://localhost:4004` in your web browser.

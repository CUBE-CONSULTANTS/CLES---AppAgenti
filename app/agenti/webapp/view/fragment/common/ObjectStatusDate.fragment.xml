<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
>
    <ObjectStatus
        class="sapUiTinyMarginTop sapUiTinyMarginEnd"
        visible="{= ${proposta>/header/mode} === 'SKIP' ? false : true }"
        text="{
          parts: ['proposta>/header/date/value', 'proposta>/header/mode'],
          formatter: '.formatter.formatDate' 
        }"
        inverted="true"
        active="false"
        state="Indication12"
    />
</core:FragmentDefinition>
